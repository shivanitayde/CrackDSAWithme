import java.util.*;
class Solution {
    public int majorityElement(int[] nums) {
       int ele=0;
       int cnt=0;
       for(int i:nums){
        if(cnt == 0)ele=i;
        cnt+= ele==i?1:-1;
       }

       return ele;
    }
}
